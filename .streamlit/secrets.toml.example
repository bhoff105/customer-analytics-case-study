# Streamlit Cloud secrets — copy this to .streamlit/secrets.toml for local development
# On Streamlit Cloud: paste these key=value pairs directly into the Secrets editor
# (App Settings > Secrets in the Streamlit Cloud dashboard)
#
# secrets.toml is in .gitignore and must NEVER be committed to the repo.

ANTHROPIC_API_KEY = "sk-ant-..."
